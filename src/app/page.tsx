import { Metadata } from "next";

export const metadata: Metadata = {
	title: "List user | User CRUD",
	description: "List user page of User CRUD App",
};

const page = () => {
	return <section className="grid h-[90dvh] place-items-center"></section>;
};

export default page;
