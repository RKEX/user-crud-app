import { Metadata } from "next";

export const metadata: Metadata = {
	title: "Create user | User CRUD",
	description: "Create user page of User CRUD App",
};
const page = () => {
	return (
		<>
			<div className=""></div>
		</>
	);
};

export default page;
